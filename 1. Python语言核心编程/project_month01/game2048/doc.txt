2048游戏
一、将game2048.py中的算法定义到游戏核心控制器GameCoreController类
    将之前的全局变量,改为实例变量
    将之前的函数,改为实例方法

二、创建生成新数字方法
    随机空位置
        -- 找出所有空位置
        -- 随机选择一个
    随机生成2(90%)或者4(10%)

三、判断游戏是否结束方法
    判断是否存在零元素(存在则游戏不结束)
    水平方向是否存在相邻相同数字(有则游戏不结束)
    垂直方向是否存在相邻相同数字(有则游戏不结束)
    以上条件都不满足,则游戏结束.

四、创建url.py模块,定义GameConsoleView类.
    游戏开始时,产生两个随机数,打印界面.
    游戏持续执行,根据输入的方向(wsad)移动地图
                产生一个随机数
                打印界面
                如果游戏结束,在终端中打印.











